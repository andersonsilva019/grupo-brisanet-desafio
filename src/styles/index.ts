import { createStitches } from '@stitches/react'

export const { globalCss, styled } = createStitches({
  theme: {
    colors: {
      'gray-100': '#E1E1E6',
      'gray-800': '#121214',
      'white': '#FFF'
    }
  }
})